SECRET_KEY=change-me
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
API_TOKEN=change-me

DB_HOST=localhost
DB_PORT=3306
DB_NAME=j46510238_12steps_app
DB_USER=j46510238_12app
DB_PASSWORD=

DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# Optional: YooKassa credentials (preferred: set in admin Settings UI)
# YOOKASSA_SHOP_ID=
# YOOKASSA_SECRET_KEY=

# Резервные копии и почта.
# Значения ниже — только значения по умолчанию: их можно задать и изменить
# в админке, раздел «Настройки» (/admin/settings). То, что задано в админке,
# важнее того, что написано в этом файле.

# Ключ шифрования архивов. Любая случайная строка. Без него приём копий выключен.
# Пример: openssl rand -hex 32
# BACKUP_ENCRYPTION_KEY=
# MAX_BACKUP_BYTES=52428800
# BACKUP_UPLOAD_COOLDOWN_SEC=600
# BACKUP_CODE_TTL_MIN=15
# BACKUP_SESSION_TTL_DAYS=180

# Почта для кода подтверждения (без неё вход в аккаунт копий недоступен).
# SMTP_HOST=smtp.yandex.ru
# SMTP_PORT=465
# SMTP_SSL=1
# SMTP_USER=noreply@12stepsapp.luch-rehab.ru
# SMTP_PASSWORD=
# SMTP_FROM=noreply@12stepsapp.luch-rehab.ru
